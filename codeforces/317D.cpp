#include <vector>
#include <list>
#include <map>
#include <set>
#include <queue>
#include <deque>
#include <stack>
#include <bitset>
#include <algorithm>
#include <functional>
#include <numeric>
#include <utility>
#include <sstream>
#include <iostream>
#include <iomanip>
#include <cstdio>
#include <cmath>
#include <cstdlib>
#include <ctime>
#include <cstring>
#include <climits>

using namespace std;

#define MAXN 35000

int N;
int F[MAXN];
bool C[MXAN];
int main()
{
	freopen("input.txt", "r", stdin);
	scanf("%d", &N);
	int mx = floor(sqrt(N));
	F[1] = 1;
	for(int i=1;i<=mx;i++)
    {
        // log(N)/log(i)
        sum = floor(log(N) / log(i));
    }
	return 0;
}
